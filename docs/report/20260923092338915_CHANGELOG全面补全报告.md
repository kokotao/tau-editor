# CHANGELOG 全面补全报告

## 结果

已重写根目录 `CHANGELOG.md`，覆盖仓库全部 18 个 Git tag，并区分 13 个公开 GitHub Release 与 5 个内部验证 tag。

## 执行范围

- 基准仓库：`kokotao/tau-editor`
- 基准文件：`CHANGELOG.md`
- 核对来源：
  - 本地 Git tag 与 tag commit
  - GitHub Releases 和 Release 资产清单
  - `docs/release/` 发布记录
  - 相邻 tag 代码差异与 `docs/report/` 交付报告
- 目标：补全每个版本的新增、改进、修复、验证、产物、限制和回滚信息。

## 主要改动

- 新增发布索引，列出版本、日期、发布状态、标签 commit 和主要变更。
- 补全 `v0.1.2`、`v0.1.3`、`v0.1.4`、`v0.1.8`、`v0.1.9` 内部验证 tag。
- 补全 `v0.2.0` 至 `v0.3.2` 缺失的公开 Release 详情。
- 保持并细化 `v0.3.3`、`v0.4.0` 的安全边界、验证结果、产物和已知限制。
- 修正 `v1.0.0` 误写：仓库不存在 `v1.0.0` tag 或 Release，旧内容已标记为未发布规划稿。
- 修正安装渠道：当前官方渠道只有 GitHub Releases，不声明未发布的包管理器渠道。
- 记录早期版本异常：
  - `v0.1.0` 是首个公开 Release，但 tag 指向 `5deca07` 时代码树已包含 `v0.1.9` 能力。
  - `v0.1.0` Release 资产名仍为 `0.1.9`，且只包含 Linux 包。
  - `v0.1.2` tag 与当时的 package version 未完全对齐。

## 验证

| 项目          | 命令或方法                                        | 结果                     |
| ------------- | ------------------------------------------------- | ------------------------ |
| Markdown 格式 | `npx prettier --check CHANGELOG.md`               | 通过                     |
| Git 空白错误  | `git diff --check`                                | 通过                     |
| 版本完整性    | 对比日志版本标题与 `git tag`                      | 18/18 一致               |
| Release 资产  | `gh release view <tag> --json assets`             | 13 个公开 Release 已核对 |
| 错误文本扫描  | 扫描 `1.0.0 - 2026`、`your-repo`、重复/错拼关键词 | 无残留                   |
| 文档落盘      | 当前报告写入 `docs/report/`                       | 完成                     |

## 风险与限制

- 早期内部 tag 没有独立 Release 说明，部分条目根据 commit diff 和后续发布记录归纳，已避免推断未在代码中出现的功能。
- 当前工作区存在未跟踪目录 `.superpowers/`，本次未修改、未删除。
- 本次只修改文档，未运行应用构建、单元测试或 E2E；Markdown 文档变更不需要桌面构建验证。
- 本次计划提交 `CHANGELOG.md` 与本报告到当前 `main` 分支；是否推送远端由后续操作决定。

## 回滚

- 仅回滚文档时，恢复 `CHANGELOG.md` 到修改前版本即可。
- 本报告为独立新增文件，删除该报告不会影响应用代码或运行行为。
- 新增 `版本条目模板`，后续版本统一使用“发布定位、新增、改进、修复、验证、发布产物、已知限制、回滚”结构。
