# 文本编辑器测试用例

## 1. 单元测试用例

### 1.1 工具函数测试

#### String Utils
| ID | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|----------|
| UT-STR-001 | 计算行数 | 输入文本 | 调用 countLines(text) | 返回正确的行数 |
| UT-STR-002 | 计算字数 | 输入文本 | 调用 countWords(text) | 返回正确的字数 |
| UT-STR-003 | 获取行内容 | 输入文本和行号 | 调用 getLine(text, lineNum) | 返回指定行的内容 |
| UT-STR-004 | 空字符串处理 | 输入空字符串 | 调用各函数 | 返回 0 或空值，不报错 |

#### File Utils
| ID | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|----------|
| UT-FILE-001 | 获取文件扩展名 | 输入文件路径 | 调用 getExtension(path) | 返回正确的扩展名 |
| UT-FILE-002 | 检测文件类型 | 输入文件名 | 调用 detectFileType(name) | 返回正确的文件类型 |
| UT-FILE-003 | 生成唯一文件名 | 输入基础名称 | 调用 generateUniqueName(base) | 返回不重复的文件名 |

#### Encoding Utils
| ID | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|----------|
| UT-ENC-001 | UTF-8 编码检测 | 输入 UTF-8 文件 | 调用 detectEncoding(buffer) | 返回 'utf-8' |
| UT-ENC-002 | GBK 编码检测 | 输入 GBK 文件 | 调用 detectEncoding(buffer) | 返回 'gbk' |
| UT-ENC-003 | 编码转换 | 输入文本和编码 | 调用 convertEncoding(text, from, to) | 返回正确编码的文本 |

### 1.2 组件测试

#### Editor 组件
| ID | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|----------|
| UT-COMP-001 | 渲染编辑器 | 无 | 渲染 Editor 组件 | 显示编辑区域和行号 |
| UT-COMP-002 | 文本输入 | 编辑器已渲染 | 输入文本 | 文本正确显示 |
| UT-COMP-003 | 行号同步 | 输入多行文本 | 滚动编辑器 | 行号同步滚动 |
| UT-COMP-004 | 语法高亮 | 输入代码 | 输入 JavaScript 代码 | 关键字高亮显示 |
| UT-COMP-005 | 光标位置 | 点击编辑区 | 点击不同位置 | 光标定位正确 |

#### Toolbar 组件
| ID | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|----------|
| UT-TOOL-001 | 按钮渲染 | 无 | 渲染 Toolbar | 显示所有工具按钮 |
| UT-TOOL-002 | 保存按钮 | 有未保存更改 | 点击保存按钮 | 触发保存操作 |
| UT-TOOL-003 | 预览按钮 | Markdown 模式 | 点击预览按钮 | 切换预览模式 |
| UT-TOOL-004 | 主题切换 | 无 | 点击主题按钮 | 切换亮暗主题 |

#### StatusBar 组件
| ID | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|----------|
| UT-STATUS-001 | 显示行列 | 编辑器有内容 | 移动光标 | 显示当前行列号 |
| UT-STATUS-002 | 显示字数 | 编辑器有内容 | 输入文本 | 实时更新字数 |
| UT-STATUS-003 | 显示编码 | 打开文件 | 读取文件编码 | 显示正确编码 |

### 1.3 Store 测试

#### Editor Store
| ID | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|----------|
| UT-STORE-001 | 初始化状态 | 无 | 创建 store | 返回默认状态 |
| UT-STORE-002 | 设置内容 | store 已创建 | 调用 setContent(text) | 内容更新 |
| UT-STORE-003 | 撤销操作 | 有多次编辑 | 调用 undo() | 回到上一状态 |
| UT-STORE-004 | 重做操作 | 执行撤销后 | 调用 redo() | 恢复撤销的内容 |
| UT-STORE-005 | 标记已保存 | 有未保存更改 | 调用 markSaved() | hasUnsavedChanges=false |

### 1.4 Hooks 测试

#### useEditor Hook
| ID | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|----------|
| UT-HOOK-001 | 初始化 | 组件挂载 | 调用 useEditor | 返回编辑器实例 |
| UT-HOOK-002 | 快捷键绑定 | hook 已初始化 | 按下 Ctrl+S | 触发保存回调 |
| UT-HOOK-003 | 清理 | 组件卸载 | unmount | 移除事件监听 |

## 2. 集成测试用例

### 2.1 文件操作
| ID | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|----------|
| IT-FILE-001 | 新建文件 | 无 | 点击新建 → 输入内容 → 保存 | 文件创建成功 |
| IT-FILE-002 | 打开文件 | 存在测试文件 | 点击打开 → 选择文件 | 文件内容正确加载 |
| IT-FILE-003 | 保存文件 | 有未保存内容 | 点击保存 | 文件更新，状态栏显示已保存 |
| IT-FILE-004 | 另存为 | 打开文件后修改 | 另存为新文件 | 原文件不变，新文件创建 |
| IT-FILE-005 | 自动保存 | 启用自动保存 | 编辑后等待 | 定时自动保存 |

### 2.2 标签页管理
| ID | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|----------|
| IT-TAB-001 | 新建标签 | 已有文件打开 | 点击新建标签 | 新标签页创建 |
| IT-TAB-002 | 切换标签 | 多个标签 | 点击不同标签 | 内容切换显示 |
| IT-TAB-003 | 关闭标签 | 多个标签 | 点击关闭按钮 | 标签关闭，切换到相邻标签 |
| IT-TAB-004 | 关闭最后一个 | 只有一个标签 | 关闭标签 | 显示空白编辑器 |

### 2.3 设置持久化
| ID | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|----------|
| IT-SET-001 | 主题持久化 | 无 | 切换主题 → 重启应用 | 主题保持 |
| IT-SET-002 | 字体持久化 | 无 | 修改字体 → 重启应用 | 字体保持 |
| IT-SET-003 | 自动保存设置 | 无 | 修改间隔 → 重启应用 | 设置保持 |

### 2.4 Markdown 预览
| ID | 用例名称 | 前置条件 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|----------|
| IT-MD-001 | 实时预览 | Markdown 文件 | 输入 Markdown | 预览区实时更新 |
| IT-MD-002 | 目录生成 | 有标题的文档 | 切换到预览 | 显示目录导航 |
| IT-MD-003 | 代码块渲染 | 有代码块 | 切换到预览 | 代码块正确渲染 |

## 3. E2E 测试用例

### 3.1 核心工作流
| ID | 用例名称 | 优先级 | 操作步骤 | 预期结果 |
|----|----------|--------|----------|----------|
| E2E-001 | 完整编辑流程 | P0 | 启动→新建→输入→保存→关闭→重新打开 | 内容完整保留 |
| E2E-002 | 多文件编辑 | P0 | 打开多个文件→切换编辑→分别保存 | 所有文件正确保存 |
| E2E-003 | 撤销重做流程 | P1 | 多次编辑→多次撤销→多次重做 | 内容正确恢复 |

### 3.2 查找替换
| ID | 用例名称 | 优先级 | 操作步骤 | 预期结果 |
|----|----------|--------|----------|----------|
| E2E-004 | 基本查找 | P1 | Ctrl+F→输入文本 | 高亮匹配项 |
| E2E-005 | 查找下一个 | P1 | 查找后点击下一个 | 跳转到下一匹配 |
| E2E-006 | 替换单个 | P1 | 查找→输入替换→替换 | 单个匹配被替换 |
| E2E-007 | 全部替换 | P1 | 查找→输入替换→全部替换 | 所有匹配被替换 |

### 3.3 Markdown 功能
| ID | 用例名称 | 优先级 | 操作步骤 | 预期结果 |
|----|----------|--------|----------|----------|
| E2E-008 | Markdown 预览 | P1 | 输入 Markdown→切换预览 | 正确渲染格式 |
| E2E-009 | 导出 HTML | P2 | 编辑 Markdown→导出 HTML | 生成正确 HTML 文件 |
| E2E-010 | 目录导航 | P2 | 长文档→点击目录项 | 跳转到对应位置 |

### 3.4 界面功能
| ID | 用例名称 | 优先级 | 操作步骤 | 预期结果 |
|----|----------|--------|----------|----------|
| E2E-011 | 主题切换 | P2 | 切换亮暗主题 | 界面颜色变化 |
| E2E-012 | 分屏编辑 | P2 | 打开分屏→同时编辑 | 两个区域独立编辑 |
| E2E-013 | 状态栏更新 | P1 | 移动光标/输入 | 状态栏实时更新 |

### 3.5 性能测试
| ID | 用例名称 | 优先级 | 操作步骤 | 预期结果 |
|----|----------|--------|----------|----------|
| E2E-014 | 大文件打开 | P2 | 打开 10MB 文件 | <5 秒完成 |
| E2E-015 | 快速输入 | P2 | 快速输入 1000 字 | 无卡顿，跟手 |
| E2E-016 | 内存占用 | P3 | 运行 1 小时 | <200MB |

## 4. Rust 端测试用例

### 4.1 Tauri Commands
| ID | 用例名称 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|
| RT-CMD-001 | read_file | 调用 read_file 命令 | 返回文件内容 |
| RT-CMD-002 | write_file | 调用 write_file 命令 | 文件写入成功 |
| RT-CMD-003 | list_files | 调用 list_files 命令 | 返回目录文件列表 |
| RT-CMD-004 | get_config | 调用 get_config 命令 | 返回配置对象 |
| RT-CMD-005 | save_config | 调用 save_config 命令 | 配置持久化 |

### 4.2 文件系统
| ID | 用例名称 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|
| RT-FS-001 | 读取 UTF-8 文件 | 读取 UTF-8 编码文件 | 正确解码 |
| RT-FS-002 | 读取 GBK 文件 | 读取 GBK 编码文件 | 正确解码 |
| RT-FS-003 | 写入文件 | 写入内容到文件 | 文件内容正确 |
| RT-FS-004 | 文件不存在 | 读取不存在文件 | 返回错误 |

### 4.3 配置管理
| ID | 用例名称 | 操作步骤 | 预期结果 |
|----|----------|----------|----------|
| RT-CFG-001 | 加载默认配置 | 无配置文件 | 返回默认配置 |
| RT-CFG-002 | 加载用户配置 | 存在配置文件 | 返回用户配置 |
| RT-CFG-003 | 保存配置 | 修改配置后保存 | 配置文件更新 |
| RT-CFG-004 | 配置验证 | 无效配置 | 使用默认值 |

## 5. 回归测试清单

每次发布前必须执行：
- [ ] E2E-001 完整编辑流程
- [ ] E2E-002 多文件编辑
- [ ] E2E-004 基本查找
- [ ] E2E-008 Markdown 预览
- [ ] E2E-011 主题切换
- [ ] IT-FILE-001 新建文件
- [ ] IT-FILE-003 保存文件
- [ ] UT-STORE-003 撤销操作
