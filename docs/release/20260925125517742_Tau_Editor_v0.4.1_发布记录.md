# Tau Editor v0.4.1 发布记录（工作台视觉与交互优化版）

> 发布时间：2026-09-25
> Release 标签：`v0.4.1`
> 基线：`v0.4.0`
> 发布仓库：https://github.com/kokotao/tau-editor

## 1. 版本定位

在 `v0.4.0` 的能力基础上，完成编辑器工作台的平角视觉统一、字号层级优化、标签交互增强、未保存状态修复与浮层收口，并加入可重复执行的视觉基线采集。

## 2. 亮点

- **平角工作台**：工具栏、标签、状态栏、文件树、上下文栏、设置页和浮层统一为平角设计。
- **字体与层级**：活动标签、路径、状态信息和设置控件统一字号与字重，中文界面更清晰。
- **未保存提示**：右上角改为单行紧凑徽标，消除中文逐字换行和挤压。
- **标签交互**：悬浮展示完整路径与状态，增加活动定位线、关闭反馈和键盘导航。
- **浮层收口**：Command Palette、设置工作区、搜索、替换预览、外部变更和通知统一视觉规范。
- **视觉基线**：新增 `npm run test:visual:baseline`，覆盖编辑器、命令面板、设置页与紧凑视口。

## 3. 发布门槛核对

| 项目 | 结果 |
|---|---|
| `npm run type-check` | 通过 |
| `npx vitest run` | 51 个文件、778 用例通过 |
| `npm run build` | 通过 |
| `npm run test:visual:baseline` | 4 张基线成功生成 |
| `RUSTFLAGS="-D warnings" cargo check --all-targets --locked` | 通过 |
| `cargo test --all-targets --locked` | 135 项通过 |
| 版本一致性 | `package.json` / `frontend/package.json` / `Cargo.toml` / `Cargo.lock` / `tauri.conf.json` 均为 0.4.1 |
| GitHub CI | 待 v0.4.1 提交推送后回填 |
| Desktop Build | 待 v0.4.1 tag 推送后回填 |

## 4. 产物清单

| 平台 | 产物 | 状态 |
|---|---|---|
| macOS | `Tau.Editor_0.4.1_aarch64.dmg` | 待构建上传 |
| Windows | `Tau.Editor_0.4.1_x64-setup.exe` | 待构建上传 |
| Windows | `Tau.Editor_0.4.1_x64_zh-CN.msi` | 待构建上传 |
| Linux | `Tau.Editor_0.4.1_amd64.deb` | 待构建上传 |
| Linux | `Tau.Editor-0.4.1-1.x86_64.rpm` | 待构建上传 |
| Linux | `Tau.Editor_0.4.1_amd64.AppImage` | 待构建上传 |

Release 地址：https://github.com/kokotao/tau-editor/releases/tag/v0.4.1

## 5. 发布执行记录

| 环节 | 编号/链接 | 结果 |
|---|---|---|
| 目标分支 | `main` | 发布准备工作进行中 |
| CI（4 门禁） | 待回填 | 待执行 |
| Desktop Build | 待回填 | 待执行 |
| Release 标签 | `v0.4.1` | 待发布 |

## 6. 升级与回滚

- 升级：直接覆盖安装即可，本次不涉及设置结构、用户数据和存储格式迁移。
- 回滚：回到 `v0.4.0` 不影响配置文件和用户数据；如出现阻断问题以 `v0.4.2` 修复，不覆盖既有 tag。
